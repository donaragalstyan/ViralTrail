# Copy to .env.local and fill in values:
#   cp .env.example .env.local

# -----------------------------------------------------------------------------
# App
# -----------------------------------------------------------------------------
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Backend API (separate server — see /backend)
NEXT_PUBLIC_API_URL=http://localhost:4000
